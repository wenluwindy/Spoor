# Spoor 不再内置任何 API Key。
# 模型服务的 Base URL / API Key / 模型一律在应用内「设置 → 模型服务」里配置，只保存在本机。

# Optional: when the project flag in src/config/persistence.ts is false, set to true to disable blur saves.
# VITE_DISABLE_CONTENT_AUTOSAVE=true
